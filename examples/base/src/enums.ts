/**
 * Custom node kinds.
 *
 * @public
 */
export enum CustomNodeKind {
  /** An article node. */
  DocArticle = 'article',
  /** A source code url node. */
  DocSource = 'source',
  /** An article toc node.  */
  DocToc = 'toc'
}
